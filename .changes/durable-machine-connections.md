- Preserve background SSH and Tailscale machine names in private per-user
  storage across updates and separate preview profiles. Recover existing
  Tailscale pairings without a new code, and allow saved machine names to be
  edited. Recovered connections wait for an explicit Connect action; credentials
  stay outside projects.
