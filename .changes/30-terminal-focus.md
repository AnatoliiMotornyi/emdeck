- Mark the focused terminal in the workspace session tabs, so split views show
  which pane receives typing without hiding the others. A maximized tab keeps
  its filled background, and a tab can be maximized and focused at once.
