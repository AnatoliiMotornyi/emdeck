- Keep dialog titles, text, fields and actions inside the window. Long paths
  wrap without scrolling confirmations sideways, and settings, connection forms
  and merge dialogs remain scrollable in small or zoomed windows.
